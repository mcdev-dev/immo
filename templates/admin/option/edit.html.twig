{% extends 'base.html.twig' %}

{% block tittle %}
    Éditer L'option
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Éditer l'option</h1>
        {{ include ('admin/option/_form.html.twig', {button: 'Sauvegarder'}) }}
    </div>
    <div class="container mt-4">
        <a href="{{ path('admin.option.index') }}" class="btn btn-warning">Annuler</a>
    </div>
{% endblock %}

